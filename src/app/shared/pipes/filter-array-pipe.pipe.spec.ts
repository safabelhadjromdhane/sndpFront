import { FilterArrayPipePipe } from './filter-array-pipe.pipe';

describe('FilterArrayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
