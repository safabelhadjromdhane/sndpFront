import { ClosingDirectiveDirective } from './closing-directive.directive';

describe('ClosingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClosingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
